# Sum of All Array Numbers - (using recursion)

## Code

```javascript
// input = [5, 3, 2, 0, 1]
// need to find the sum of all the numbers in the array (using recursion)

const arr = [5, 3, 2, 0, 1];

const sum = (n) => {
  if (n == 0) return arr[n];
  return arr[n] + sum(n - 1);
};

console.log(sum(arr.length - 1));
```

## Overall Algorithm Logic

```mermaid
graph TD
    A["🟢 START<br/>arr=[5,3,2,0,1]<br/>n=4"] --> B{"n == 0?"}
    B -->|NO| C["return arr[4] + sum(3)<br/>1 + sum(3)"]
    C --> D["Call sum(3)"]
    D --> E{"3 == 0?"}
    E -->|NO| F["return arr[3] + sum(2)<br/>0 + sum(2)"]
    F --> G["Call sum(2)"]
    G --> H["Call sum(1)"]
    H --> I["Call sum(0)"]
    I --> J{"0 == 0?"}
    J -->|YES| K["return arr[0]<br/>Base case: 5"]
    K --> L["Unwind stack<br/>sum(0) = 5"]
    L --> M["sum(1) = arr[1] + 5<br/>3 + 5 = 8"]
    M --> N["sum(2) = arr[2] + 8<br/>2 + 8 = 10"]
    N --> O["sum(3) = arr[3] + 10<br/>0 + 10 = 10"]
    O --> P["sum(4) = arr[4] + 10<br/>1 + 10 = 11"]
    P --> Q["🔴 Return 11<br/>Final result"]
```

## Example Walkthrough

For arr = [5, 3, 2, 0, 1]:

- sum(4) = arr[4] + sum(3) = 1 + sum(3)
- sum(3) = arr[3] + sum(2) = 0 + sum(2)
- sum(2) = arr[2] + sum(1) = 2 + sum(1)
- sum(1) = arr[1] + sum(0) = 3 + sum(0)
- sum(0) = arr[0] = 5 (base case)
- Unwinding: 3 + 5 = 8, 2 + 8 = 10, 0 + 10 = 10, 1 + 10 = 11

**Result:** 11

## Complexity Analysis

- **Time Complexity:** O(n) - Each call makes one recursive call until n=0
- **Space Complexity:** O(n) - Recursion stack depth is n
- **Note:** This demonstrates recursion but is less efficient than iterative approach
